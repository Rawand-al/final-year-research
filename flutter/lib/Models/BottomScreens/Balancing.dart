import 'package:flutter/material.dart';

class balancing extends StatefulWidget {
  const balancing({super.key});

  @override
  State<balancing> createState() => _balancingState();
}

class _balancingState extends State<balancing> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(      appBar: AppBar(title:Center(child: const Text("Balancing Page")),),
);
  }
}